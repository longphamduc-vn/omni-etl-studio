[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "omni-etl-studio"
version = "0.1.0"
dependencies = [
    "streamlit",
    "pandas",
    "duckdb",
    "pydantic",
    "pydantic-settings"
]

[tool.setuptools.packages.find]
where = ["."]